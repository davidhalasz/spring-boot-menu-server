export default class Menu {
  constructor(id, userId, types, products, prices) {
    this.id = id;
    this.userId = userId;
    this.types = types;
    this.products = products;
    this.prices = prices;
  }
}
